import { Card, CardProps } from './Card'
import { Header, HeaderProps } from './Header'
import { Search, SearchProps } from './Search'

export const Templates = {
  Card,
  Header,
  Search,
}

export type { CardProps, HeaderProps, SearchProps }
